# The URL the site will be built for
base_url = "https://redstoneparadox.xyz"

# A list of glob patterns specifying asset files to ignore when the content
# directory is processed. Defaults to none, which means that all asset files are
# copied over to the `public` directory.
# Example:
#     ignored_content = ["*.{graphml,xlsx}", "temp.*", "**/build_folder"]
ignored_content = ["*.json"]

# When set to "true", a feed is automatically generated.
generate_feed = true

taxonomies = [
    { name = "tags", paginate_by=10 }
]

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
# Put all your custom variables here
socials = [
        { name="Github", url="https://github.com/RedstoneParadox" },
        { name="YouTube", url="https://github.com/RedstoneParadox" },
        { name="Modrinth", url="https://github.com/RedstoneParadox" },
        { name="Discord", url="https://github.com/RedstoneParadox" },
]

nav_links = [
    { name="Home", path="/" },
    { name="Projects", path="/projects/" },
    { name="Blog", path="/blog/" },
    { name="Maven", url="https://maven.redstoneparadox.xyz"}
]
